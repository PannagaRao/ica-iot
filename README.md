# modbus_pdf_excel
connect modbus library with sqlalchemy-sqlite db data export to pdf as excel format python flask
in terminal, run this command
venv\Scripts\activate
pip install -r requirements.txt
python client_new.py
